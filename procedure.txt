1. Backend URL: http://127.0.0.1:8000
   Swagger: http://127.0.0.1:8000/docs

2. PostgreSQL Database
Database Name:forensic_ai
Username:postgres
Password:admin
Host:localhost
Port: 5432

Connection String:
postgresql://postgres:admin@localhost:5432/forensic_ai
Verify:

\dt

Expected Tables:
users
cases
evidence
custody
findings

3. Environment Variables (.env)

You previously showed: DATABASE_URL=postgresql://postgres:admin@localhost:5432/forensic_ai

ALGORITHM=HS256

ACCESS_TOKEN_EXPIRE_MINUTES=60

For security, do NOT share:

OPENAI_API_KEY
SECRET_KEY
JWT SECRET

in reports or screenshots.

4. User Accounts
Currently your backend supports:

Admin
Investigator
Student

But unless we have already created those users in the database, they won't exist yet.

Check users:

SELECT id,name,email,role
FROM users;
5. Registration API

Endpoint:
POST /auth/register

Example:

{
  "name": "jeetendar",
  "email": "admin@forensicai.com",
  "password": "Admin@123",
  "role": "admin"
}

Response:

{
  "message": "User Registered"
}

6. Login API

Endpoint:

POST /auth/login

Example:

{
  "email": "admin@forensicai.com",
  "password": "Admin@123"
}

Response:

{
  "access_token": "...",
  "token_type": "bearer"
}
7. Cases API

Create Case:

POST /cases/create

Example:

{
  "case_name": "Mobile Fraud Investigation",
  "description": "UPI Fraud Case"
}
8. Evidence Upload API
POST /evidence/upload

Form Data:

case_id
evidence_type
file

Example:

case_id = 1
evidence_type = Image
file = pu3.png

Response:

{
  "message": "Evidence Uploaded",
  "sha256": "...",
  "metadata": {...},
  "analysis": "...",
  "report": "reports/2.pdf"
}
9. Findings API

Retrieve Findings:

GET /findings/{evidence_id}

Example:

GET /findings/2
10. Report Download
GET /reports/{report_id}

Example:

GET /reports/2

Downloads:

reports/2.pdf
11. Chain of Custody

Add:

POST /custody/add

Example:

{
  "evidence_id": 2,
  "officer_name": "Jeetendar",
  "action_taken": "Evidence Collected",
  "location": "Cyber Lab",
  "remarks": "Mobile device received"
}

View:

GET /custody/2
12. Timeline

Generate Timeline:

GET /timeline/2

Output:

{
  "timeline": [...],
  "summary": "DIGITAL FORENSICS TIMELINE..."
}
13. Current Project Status
Backend APIs            ✅
Authentication          ✅
Cases                   ✅
Evidence Upload         ✅
SHA256                  ✅
Metadata Extraction     ✅
AI Analysis             ✅
Findings                ✅
Chain of Custody        ✅
Timeline                ✅
PDF Reports             ✅

Frontend               Before React Step 2

One thing I need from you:

Open PostgreSQL and run:

SELECT id,name,email,role
FROM users;



import { useEffect, useState } from "react";
import {
  getUsers,
  createUser
} from "../services/userService";

import {
  getCases,
  createCase
} from "../services/caseService";

export default function AdminDashboard() {

  const [users, setUsers] = useState([]);
  const [userCount, setUserCount] = useState(0);
  const [investigatorCount, setInvestigatorCount] = useState(0);
const [activePanel, setActivePanel] = useState("dashboard");

const [name, setName] = useState("");
const [email, setEmail] = useState("");
const [password, setPassword] = useState("");
const [role, setRole] = useState("Investigator");
const [cases, setCases] = useState([]);

const [caseTitle, setCaseTitle] = useState("");

const [caseType, setCaseType] = useState("");

const [description, setDescription] = useState("");

const [assignedInvestigator, setAssignedInvestigator] = useState("");

const [priority, setPriority] = useState("High");

const [status, setStatus] = useState("Open");

  
  useEffect(() => {

  loadUsers();

  loadCases();

}, []);
 const loadUsers = async () => {

  try {

    const data = await getUsers();

    setUsers(data);

    setUserCount(data.length);

    const investigators = data.filter(
      (user) => user.role === "Investigator"
    );

    setInvestigatorCount(
      investigators.length
    );

  }

  catch (error) {

    console.log(error);

  }

};

const handleCreateUser = async () => {

  try {

    const result = await createUser({

      name,
      email,
      password,
      role

    });

    alert(
      `User Created\n\nUser ID: ${result.user_id}`
    );

    setName("");
    setEmail("");
    setPassword("");

    loadUsers();
const loadCases = async () => {

  try {

    const data = await getCases();

    setCases(data);

  }

  catch(error){

    console.log(error);

  }

};
  }

  catch(error){

    console.log(error);

    alert("Failed To Create User");

  }

};
   

  

  return (

    <div className="min-h-screen bg-[#0A0F1F] text-white flex">

      {/* Sidebar */}

      <div className="w-64 bg-[#111827] p-6 border-r border-cyan-900">

        <h1 className="text-2xl font-bold text-cyan-400">
          ForensicAI
        </h1>

        <p className="text-gray-500 text-sm mt-1">
          Admin Console
        </p>

        <div className="mt-10 space-y-5">

         <div
  onClick={() => setActivePanel("dashboard")}
  className="hover:text-cyan-400 cursor-pointer"
>
  Dashboard
</div>

      <div
  onClick={() => setActivePanel("users")}
  className="hover:text-cyan-400 cursor-pointer"
>
  User Management
</div>

       <div
  onClick={() => setActivePanel("cases")}
  className="hover:text-cyan-400 cursor-pointer"
>
  Cases
</div>
         <div
  onClick={() => setActivePanel("evidence")}
  className="hover:text-cyan-400 cursor-pointer"
>
  Evidence
</div>

      <div
  onClick={() => setActivePanel("reports")}
  className="hover:text-cyan-400 cursor-pointer"
>
  Reports
</div>

          <div className="hover:text-red-400 cursor-pointer">
            Logout
          </div>

        </div>

      </div>

      {/* Main Content */}

      <div className="flex-1 p-8">
{
  activePanel === "users" && (

    <div>

      <h2 className="text-4xl font-bold text-cyan-400">

        User Management

      </h2>

      <p className="text-gray-400 mt-2">

        Manage Students, Investigators and Admins

      </p>

      <div className="bg-[#111827] p-6 rounded-xl mt-8">

        <h3 className="text-2xl font-bold text-cyan-400 mb-4">

          Create User

        </h3>

        <input
  value={name}
  onChange={(e)=>setName(e.target.value)}
  placeholder="Full Name"
          className="w-full p-3 bg-[#0A0F1F] rounded mb-4"
        />

        <input
  value={email}
  onChange={(e)=>setEmail(e.target.value)}
  placeholder="Email Address"
          className="w-full p-3 bg-[#0A0F1F] rounded mb-4"
        />

      <input
  type="password"
  value={password}
  onChange={(e)=>setPassword(e.target.value)}
  placeholder="Password"
          className="w-full p-3 bg-[#0A0F1F] rounded mb-4"
        />

       <select
  value={role}
  onChange={(e)=>setRole(e.target.value)}
          className="w-full p-3 bg-[#0A0F1F] rounded mb-4"
        >

          <option>Investigator</option>

          <option>Student</option>

          {/* <option>Admin</option> */}

        </select>

      <button
  onClick={handleCreateUser}
  className="bg-cyan-500 px-6 py-3 rounded font-bold"
>
          Create User
        </button>

      </div>

    </div>

  )
}
{
  activePanel === "dashboard" && (
    <div>
        <h2 className="text-4xl font-bold text-cyan-400">
          Admin Dashboard
        </h2>

        <p className="text-gray-400 mt-2">
          Digital Forensics Investigation Control Center
        </p>

        {/* Statistics */}

        <div className="grid grid-cols-4 gap-6 mt-10">

          <div className="bg-[#111827] p-6 rounded-xl shadow-lg">

            <h3 className="text-gray-400">
              Total Users
            </h3>

            <p className="text-3xl font-bold mt-2">
              {userCount}
            </p>

          </div>

          <div className="bg-[#111827] p-6 rounded-xl shadow-lg">

            <h3 className="text-gray-400">
              Investigators
            </h3>

            <p className="text-3xl font-bold mt-2">
              {investigatorCount}
            </p>

          </div>

          <div className="bg-[#111827] p-6 rounded-xl shadow-lg">

            <h3 className="text-gray-400">
              Cases
            </h3>

            <p className="text-3xl font-bold mt-2">
              0
            </p>

          </div>

          <div className="bg-[#111827] p-6 rounded-xl shadow-lg">

            <h3 className="text-gray-400">
              Evidence
            </h3>

            <p className="text-3xl font-bold mt-2">
              0
            </p>

          </div>

        </div>

        {/* Recent Activities */}

        <div className="bg-[#111827] rounded-xl p-6 mt-10">

          <h3 className="text-xl font-bold text-cyan-400">
            Recent Activities
          </h3>

          <p className="text-gray-500 mt-4">
            No recent activities available.
          </p>

        </div>
  
        {/* Users Table */}
    </div>

  )
}
        <div className="bg-[#111827] rounded-xl p-6 mt-8">

          <h3 className="text-xl font-bold text-cyan-400 mb-4">
            Registered Users
          </h3>
  
          <table className="w-full">

            <thead>

              <tr className="text-left border-b border-gray-700">

                <th className="pb-2">User ID</th>
                <th className="pb-2">Name</th>
                <th className="pb-2">Role</th>

              </tr>

            </thead>

            <tbody>

              {
                users.map((user) => (

                  <tr
                    key={user.id}
                    className="border-b border-gray-800"
                  >

                    <td className="py-2">
                      {user.user_id}
                    </td>

                    <td>
                      {user.name}
                    </td>

                    <td>
                      {user.role}
                    </td>

                  </tr>

                ))
              }

            </tbody>

          </table>

        </div>
  
    </div>
            
    </div>
            
  );
}
